Create :
CREATE DATABASE - создает базу данных с указанным именем.

Insert:
Команда INSERT INTO  в SQL отвечает за добавление данных в таблицу:

INSERT При добавлении данных в каждый столбец таблицы не требуется указывать названия столбцов.


Select:
позволяет работать непосредственно с данными для получения данных из выбранной таблицы


Update:
UPDATE таблица SET меняет выбраные значения в элементах таблицы на другие данны


Drop:
DROP DATABASE [IF EXISTS] db_name - удаляет все таблицы в указанной базе данных и саму базу.